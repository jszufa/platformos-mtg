---
slug: image/create_record
method: post
---

{% liquid
	graphql result = 'image/create', direct_url: context.params.direct_url
    
	if result.record.id
		redirect_to '/'
	else
		echo 'Something went wrong'
		echo result.errors
	endif
%}