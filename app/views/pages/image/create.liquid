---
method: post
---

{% liquid
	graphql result = 'image/create', direct_url: context.params.location
    
	if result.record.id
		redirect_to '/'
	else
		echo 'Something went wrong'
		echo result.errors
	endif
%}